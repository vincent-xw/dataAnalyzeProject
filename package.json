{
  "name": "data-analyze-project",
  "private": true,
  "packageManager": "pnpm@11.9.0",
  "engines": {
    "node": "22.23.1"
  },
  "scripts": {
    "validate:scripts": "node --import tsx scripts/validate-script-registry.ts",
    "typecheck": "pnpm -r typecheck",
    "test": "vitest run scripts/validate-script-registry.test.ts && pnpm -r test",
    "build": "pnpm -r build"
  },
  "devDependencies": {
    "tsx": "4.23.1",
    "typescript": "7.0.2",
    "vitest": "4.1.10"
  }
}
