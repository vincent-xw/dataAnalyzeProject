# 分析选表预览设计

在顶层“数据分析”的新建表单中，用户选中数据表后，前端请求既有 `/api/assets/:id/preview` 接口并展示列名及前十行。多表预览并发加载，各自显示加载或失败状态；主表继续使用现有选择器标识。预览只用于辅助填写自然语言需求，创建请求仍只提交资产 ID、主表 ID 和需求，不向模型发送数据行。

不新增后端接口、存储或迁移。测试覆盖选表后请求预览并渲染前十行。
