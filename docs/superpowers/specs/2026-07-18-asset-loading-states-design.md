# 数据资产加载状态设计

资产列表在首次请求期间显示“正在加载数据资产…”，仅在请求完成且结果为空时显示空状态。上传页在网络请求期间禁用文件、格式和工作表控件；CSV 上传显示“正在上传并转换…”，XLSX 首次读取显示“正在读取工作表…”，选表后转换显示“正在转换所选工作表…”。失败时恢复可操作状态并保留错误提示。

测试覆盖列表加载占位、上传期间禁用和对应按钮文案，以及请求失败后的恢复状态。
